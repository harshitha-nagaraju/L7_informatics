# Database file path (SQLite)
DATABASE_URL=sqlite:///./data/expenses.db


# Email (optional) - configure to enable email alerts
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=youruser
SMTP_PASS=yourpass
EMAIL_FROM=alerts@example.com


# Alert threshold: fraction of remaining budget to trigger "low budget" alert (0.1 = 10%)
ALERT_THRESHOLD=0.1

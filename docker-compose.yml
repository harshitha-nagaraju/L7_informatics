
version: "3.9"

services:
  web:
    build: .
    container_name: expense-tracker
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - DATABASE_URL=sqlite:///data/expenses.db
    volumes:
      - .:/app
